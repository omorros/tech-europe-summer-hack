.env
.venv/
__pycache__/
# agent screenshots and timestamped approach shots are per-run noise;
# static/artifacts (golden gallery + floorplan) IS committed so the cached
# demo fallback works from a fresh clone
static/agent/
static/approach/
