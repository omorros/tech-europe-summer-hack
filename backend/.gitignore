.env
.venv/
__pycache__/
# agent screenshots are per-run noise; static/artifacts (golden gallery +
# floorplan) and static/approach (imagery referenced by cached approach.json)
# ARE committed so the cached fallback works from a fresh clone
static/agent/
